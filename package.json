{
  "name": "tmchat",
  "version": "1.0.0",
  "main": "index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "npm run start --prefix backend",
    "heroku-postbuild": "npm config set fund false && npm config set audit false && npm install --prefix backend && npm install --prefix frontend --include=dev && npm run build --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
